import React from "react"
import styled from "styled-components"

const ContactInfoDiv = styled.div``

const ContactInfo = ({ data }) => {
  return <ContactInfoDiv></ContactInfoDiv>
}

export default ContactInfo
