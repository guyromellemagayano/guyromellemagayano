import { NotFoundApp } from '@portfolio/components'

/**
 * Render the custom not found app page.
 * @returns The rendered custom not found app page
 */
const NotFoundPage = async () => {
  return <NotFoundApp />
}

export default NotFoundPage
