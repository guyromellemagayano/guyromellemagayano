import { LoadingApp } from '@portfolio/components'

/**
 * Render the loading page
 * @returns The rendered page
 */
const Loading = () => {
  return <LoadingApp />
}

Loading.displayName = 'Loading'

export default Loading
